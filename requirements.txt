fastapi==0.110.0
uvicorn==0.29.0
asyncpg==0.29.0
redis==5.0.3
pydantic==2.6.4
aiohttp==3.9.3
httpx==0.27.0
python-multipart==0.0.9
python-telegram-bot[job-queue]==21.0.1

# Wheel segmentation (Stage 1 — src/wheel_seg.py).
# Pure ONNX runtime + numpy + Pillow без torch/ultralytics — критично
# для Render Free 512MB RAM.
numpy==1.26.4
Pillow==10.3.0
onnxruntime==1.17.3

# Local fal.ai masked inpainting evaluation runner
# (scripts/fal_inpaint_eval.py; paid calls require --execute).
fal-client==1.0.0
